
export function Footer() {
    return (
        <footer className="bg-gray-800 text-white py-2 text-center w-full mt-auto fixed bottom-0">
            <p>© {new Date().getFullYear()} NovoCode - Todos os direitos reservados.</p>
        </footer>
    );
}